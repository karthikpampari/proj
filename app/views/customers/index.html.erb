<p style="color: green"><%= notice %></p>

<h1>Customers</h1>

<div id="customers">
  <% @customers.each do |customer| %>
     <p>
      <strong>Email:</strong>
      <%= customer.email %>
      </p>

    <p>
      <%= link_to "Show this customer", customer_path(customer) %>
    </p>
  <% end %>
</div>

<%= link_to "New customer", new_customer_path %>
<%= link_to "back", root_path %>

